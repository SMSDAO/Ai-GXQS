# config/nginx.conf
server {
    listen 80;
    server_name smartprompt.elite;

    location / {
        proxy_pass http://dashboard:3000;
        proxy_set_header Host $host;
    }

    location /api {
        proxy_pass http://api:8080;
        proxy_set_header Host $host;
    }
}
